### TODO 2: Core Layout & Navigation (Sidebar + Floating AI Button)
*   **Task:** Build the Dashboard layout framework with a responsive sidebar and a global floating chat button.
*   **Prompt for Copilot:**
    > "Create a layout component for `/dashboard`. It must include a left-side navigation sidebar containing links for: Dashboard, Treatment Guidance, Medicine Interaction, Risk Detection, Disease Detection, Vaccination Reminders, and Settings. Add a persistent, absolute-positioned floating chat action button (FAB) in the bottom-right corner of the screen with a chat icon. Clicking it should trigger a slide-out drawer or overlay for the 'Ask AI' assistant. Ensure the layout is highly responsive. Update CONTEXT.md and mark TODO 2 as complete in TODO.md."



# Core Dashboard Layout, Navigation Sidebar & Persistent Floating AI Drawer

This document provides absolute, production-grade instructions to implement the core workspace shell for the Health Worker Assistant (`app/(dashboard)/layout.tsx`). This includes a responsive, high-contrast left navigation sidebar and a global floating action button (FAB) that opens an interactive, overlaying AI sliding drawer.

---

## 1. Architectural Layout Blueprint
The protected dashboard uses a two-column layout grid system on desktop that gracefully shifts into an overlay layout on mobile devices to preserve extreme readability for field healthcare professionals working in rough environments.

```text
+-----------------------------------------------------------------------+
|  SIDEBAR            |  (PINNED ACTIVE PATIENT BANNER - TODO 4)        |
|  [Logo & Title]     +-------------------------------------------------+
|                     |                                                 |
|  - Intake Session   |                                                 |
|  - Guidance         |         DYNAMIC ROUTE VIEW CONTENTS             |
|  - Diagnostics      |         (app/(dashboard)/* sub-pages)           |
|  - Interactions     |                                                 |
|  - Reminders        |                                                 |
|  - RAG Settings     |                                                [FAB]
|                     |                                                [ AI]
+---------------------+-------------------------------------------------+

```

---

## 2. Technical Component Specifications

### A. Core Dashboard Layout (`app/(dashboard)/layout.tsx`)

* **Grid System:** On desktop viewports (min-width: 1024px), it instantiates a strict two-column grid. The first column fixes the Sidebar configuration permanently to the left wall (`w-64` or `w-72`). The remaining layout expands smoothly to handle sub-route panels (`flex-1`).
* **Mobile Adaptability:** On smaller screen viewports, the left sidebar slides completely out of view behind a toggleable hamburger button menu to maximize workspace real estate.
* **Layer Ordering (Z-Index Hierarchy):**
* Sub-page text blocks: `z-0`
* Sticky Session Banner (built in upcoming steps): `z-10`
* Mobile Hamburger/Left Navigation Side Column: `z-40`
* Floating Chat Drawer (Sheet) & Overlay backdrop: `z-50`



### B. Navigation Sidebar Component (`components/sidebar.tsx`)

* **Color Palette Tokens:** Extracted explicitly from `/ignore/THEME_REF.html` design requirements:
* Base panel background: Slate dark slate or high-contrast clean surface border color (`bg-slate-900` text white or `bg-white` borders slate-200). Let's implement deep slate medical primary theme tracking (`bg-slate-900`, borders `slate-800`).
* Active links: Highlighted using high-visibility medical sky-blue/teal backgrounds (`bg-sky-600` or `bg-sky-700`) with absolute crisp typography to maintain immediate utility outdoors.


* **Required Navigation Links & Target Paths:**
1. **👤 Patient Intake Session:** `/dashboard/session` (The core gateway for starting clinical evaluations)
2. **📋 Treatment Guidance:** `/dashboard/guidance` (Step-by-step interactive procedures tracker)
3. **🩺 Diagnostics Suite:** `/dashboard/diagnostics` (Unified Risk scoring and Disease differential maps)
4. **💊 Rx Interactions:** `/dashboard/interactions` (Cross-prescription biochemical safety screening)
5. **📅 Immunization Reminders:** `/dashboard/reminders` (Automated tracking ledger)
6. **⚙️ Knowledge & Settings:** `/dashboard/settings` (RAG document vector upload management control zone)



### C. Persistent Floating AI Button & Sliding Sheet (`components/floating-ai-chat.tsx`)

* **The Trigger FAB:** Anchored strictly using CSS `fixed bottom-6 right-6`. Styled as an expansive circular active target button (`h-14 w-14 rounded-full`) wrapped inside a crisp pulse outline effect to draw tactical attention without occluding text data arrays below it.
* **The Interface Container:** Triggered natively using the Shadcn/UI `<Sheet>` or `<SheetContent side="right">` drawer controller mechanism. It opens an overlay panel layout covering 400px of view space on desktop, scaling to full width on mobile viewports.
* **Internal Layout State Placeholder:** For this step, embed a loading layout frame with clear clinical statement banners: *"RAG Inference Core Online - Grounded to Vector Knowledge Base chunks"*.

---

## 3. Comprehensive Implementation Prompt for Your AI/Copilot

Copy and paste this explicit instructions packet into your code generator to build the interface shell immediately:

```markdown
Read INITIAL.md, CONTEXT.md, and STEP_02_DATABASE_AND_TYPES.md before writing any code.

[OBJECTIVE]
Implement TODO 2: Build the core dashboard structural shell, including the left-side navigation navigation sidebar and a persistent bottom-right floating action button (FAB) that opens the Right Drawer Sheet for our upcoming RAG-powered clinical assistant.

[TECHNICAL SEQS & REGIONS]
1. Create a master structural workspace layout at `app/(dashboard)/layout.tsx` wrapping all nested child routing nodes.
2. Build the Left Navigation Sidebar component inside `components/sidebar.tsx` matching the clean medical dark-slate theme characteristics profile outlined in `/ignore/THEME_REF.html`. Map out the exact list of clinical sub-route links (`/dashboard/session`, `/dashboard/guidance`, `/dashboard/diagnostics`, `/dashboard/interactions`, `/dashboard/reminders`, `/dashboard/settings`) along with crisp Lucide React icons for each entry (Users, FileText, Activity, Pill, Calendar, Settings).
3. Implement absolute path detection tracking using the Next.js `usePathname()` utility hook. Highlight the currently active link element dynamically with a highly visible sky-blue tint (`bg-sky-600` text-white) to optimize field tracking clarity.
4. Construct a persistent, absolute fixed-position interactive floating action button component inside `components/floating-ai-chat.tsx` fixed firmly to `bottom-6 right-6`. Give it a prominent medical icon overlay wrapper.
5. Wire this FAB button up to toggle the Shadcn/UI Sheet element framework. Ensure the sheet panel slides out cleanly from the right screen boundary (`side="right"`), handles absolute focus escape loops safely, and maps out a temporary placeholder text structure inside reading "RAG Knowledge Vector Client Context Engine Loader coming soon...".
6. Wrap all layouts in highly adaptable Tailwind configurations so everything handles compact portable screen displays dynamically without breaking or hiding data containers.

[POST-CONDITION]
Once the file paths compile cleanly without hydration runtime errors:
- Document the design layout elements within the Architecture segment of CONTEXT.md.
- Open TODO.md and mark [x] TODO 2 as complete.
